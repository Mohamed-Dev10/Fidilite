@{
    ViewData["Title"] = "Utilisateurs";
    var userWithRoles = ViewBag.UserWithRoles as List<(BRICOMA.ECOMMERCE.Data.ApplicationUser.ApplicationUser User, string Role)> ?? new();
}

<div class="page-header mb-3">
    <div class="row align-items-center">
        <div class="col">
            <h2 class="page-title">Utilisateurs back-office</h2>
        </div>
        <div class="col-auto">
            <a href="@Url.Action("CreateUser")" class="btn btn-primary">
                <i class="ti ti-plus me-1"></i> Nouvel utilisateur
            </a>
        </div>
    </div>
</div>

<div class="card">
    <div class="table-responsive">
        <table class="table table-vcenter table-hover card-table">
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Rôle</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (!userWithRoles.Any())
                {
                    <tr><td colspan="3" class="text-center text-muted py-4">Aucun utilisateur</td></tr>
                }
                else
                {
                    foreach (var (user, role) in userWithRoles)
                    {
                        <tr>
                            <td>@user.Email</td>
                            <td>
                                <span class="badge bg-blue-lt">@role</span>
                            </td>
                            <td>
                                <a href="@Url.Action("EditUser", new { id = user.Id })" class="btn btn-sm btn-outline-secondary">
                                    <i class="ti ti-edit me-1"></i> Modifier
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
